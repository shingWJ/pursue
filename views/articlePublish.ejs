<% include header.ejs %>
<header class="header-latest jumbotron">
  <div class="navigation">
    <a href="/index">点滴</a>
    <span>/</span>
    <a href="/articleList">菏泽</a>
  </div>
  <div class="container">
    <h2>雁过留痕</h2>
    <h3>天空没留下我的足迹，但你确已飞过</h3>
  </div>
</header>
<% if (success) { %>
  <div class="sucess"><%= success %></div>
<% } %>
<% if (error) { %>
  <div class="error"><%= error %> </div>
<% } %>
<!-- 加载编辑器的容器 -->
    <!--<script id="container" name="content" type="text/plain">
        这里写你的初始化内容
    </script>-->
    <!-- 配置文件 -->
    <script type="text/javascript" src="/kindeditor/kindeditor-all.js"></script>
    <form method="post">
      <div class="form-group sub-title">
      <label for="topic">主题</label>
      <select class="form-control" id="topic" name="topic">
        <option value="article">文章</option>
        <option value="happiness">开心一刻</option>
      </select>
    </div>
    <div class="form-group sub-title">
    	<label for="subTitle">标题</label>
    	<input type="subTitle" class="form-control" id="subTitle" name="subTitle" placeholder="标题，小小总结一下">
  	</div>
    <div class="article-write" id="container">
    <textarea id="editor" name="content" style="width:100%;height:320px">
    </textarea>
    </div>
    <input name="val" id="val" type="hidden"></input>
    <button type="submit" class="btn btn-primary pub-btn">提交</button>
    </form>
    <script type="text/javascript">
     	var options = {
              uploadJson: '/uploadImg'
        };
        KindEditor.ready(function(K) {
              window.editor = K.create('#editor', options);
        });
        //ar ue = UE.getEditor('container');
        //document.getElementById('val').value=ue.body.children[0];
    </script>
<% include footer.ejs %>